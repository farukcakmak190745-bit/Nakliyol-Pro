rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Test Mode - Geliştirme için açık
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
